@model DoktorPretragaVM
@{
    int i = 1;
}

<div class="container">

    <div class="row">
        <div class="col-md-4 col-sm-4 col-xs-12 picDiv">
            <img src="~/images/logo.png" class="img-responsive img-circle logoPicture" />
        </div>
        <div class="col-md-8 dodajHeaderDiv">
            <h1>Pretraga doktora</h1>
        </div>
    </div>
    <div class="container">

        <form>
            <div class="row form-group">
                <label asp-for="@Model.Text" class="col-md-1" style="vertical-align:middle;">Pretraga:</label>
                <div class="col-md-5">
                    <input asp-for="@Model.Text" class="form-control" />
                    <label style="font-size:10px;">(unesite ime i prezime doktora)</label>
                </div>
                <button type="submit" class="btn btn-danger">
                    <i class="fas fa-search icons-margin-right"></i> Traži
                </button>
            </div>
        </form>
        <div class="row">
            <table class="table table-condensed">
                <thead>
                <th scope="row"></th>
                <th scope="row">Ime i prezime</th>
                <th scope="row">Datum rodjenja</th>
                @*<th scope="row">Akcija</th>*@
                </thead>
                <tbody>
                    @foreach (var item in Model.Doktori)
                    {
                        <tr>
                            <th>@i</th>
                            @{
                                i += 1;
                            }
                            <td>@(item.Osoblje.Korisnici.Ime + " " + item.Osoblje.Korisnici.Prezime)</td>
                            <td>@item.Osoblje.Korisnici.DatumRodjenja.ToShortDateString()</td>
                            @*<td>
                                <a asp-action="Detalji" asp-controller="Doktor" asp-route-DoktorId="@item.Id" class="btn btn-danger"><i class="fas fa-user icons-margin-right"></i>Profil</a>
                                <a asp-action="Uredi" asp-controller="Doktor" asp-route-id="@item.Id" class="btn btn-danger"><i class="fas fa-wrench icons-margin-right"></i>Uredi</a>
                                <a asp-action="Izbrisi" asp-controller="Doktor" asp-route-id="@item.Id" class="btn btn-danger"><i class="fas fa-trash-alt icons-margin-right"></i>Obriši</a>
                            </td>*@
                        </tr>
                    }
                </tbody>

            </table>
        </div>
    </div>
</div>